{
  "name": "conselhos-esotericos",
  "version": "1.0.0",
  "description": "Plataforma de consultas esotéricas online",
  "main": "server/index.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && nodemon index.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "node server/index.js",
    "postinstall": "cd server && npm install --production && cd ../client && npm install && npm run build"
  },
  "keywords": ["esoteric", "consultations", "platform", "nodejs", "express", "react", "vite"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
